<form method="post" action="{{ path('front_company_app_category_delete', {'id': categoryId}) }}" class="block px-4 py-2 hover:bg-neutral-100 dark:hover:bg-neutral-600 dark:hover:text-white" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categoryId) }}">
	<button class="btn font-bold text-red-600">Supprimer</button>
</form>
